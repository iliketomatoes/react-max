import { createStyles } from '@material-ui/core';

const styles = createStyles({
  root: {
    display: 'block',
    margin: '0 auto',
    padding: '20px',
    width: '100%'
  }
});

export default styles;
